// message_log.cpp
// This project historically referenced message_log.cpp in the workspace.
// Some source trees may be missing the file, which breaks the build.
// The current codebase does not require any symbols from this unit,
// but we keep it to satisfy the project file list under eVC++ 3.0.

#include "StdAfx.h"

// Intentionally empty.
